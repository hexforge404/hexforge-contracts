# Pack Engine ENV — Contract

Pack is a packaging layer for bundling outputs (zip, manifests).

## Required
- `PACK_OUTPUT_DIR` (disk location for pack results)
- `PACK_PUBLIC_PREFIX` (usually `/assets/pack`)
