# Surface (Legacy Name) — ENV

This folder exists because the public route remains `/api/surface/*`.

Internally the engine is now `glyphengine`.
Use `engines/glyphengine/*` as authoritative contracts.
