# Auditor Interfaces — Contract

Auditor is a CLI + optional service.

## CLI commands (recommended)
- `auditor scan` → runs all checks
- `auditor scan --engine glyphengine` → engine-only checks
- `auditor scan --infra nginx` → nginx/mount/url checks

## Output locations
- writes reports under: `/mnt/hdd-storage/ai-tools/engines/hexforge3d/audit/`
